import { HostingDirective } from './hosting.directive';

describe('HostingDirective', () => {
  it('should create an instance', () => {
    const directive = new HostingDirective();
    expect(directive).toBeTruthy();
  });
});
